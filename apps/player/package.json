{
  "name": "@rpgcrafter/player",
  "private": true,
  "version": "0.6.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4173",
    "build": "vite build",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1 --port 4173"
  },
  "dependencies": {
    "@rpgcrafter/game-schema": "workspace:*",
    "pixi.js": "^8.19.0"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vite": "^8.1.4",
    "vitest": "^4.0.18"
  }
}
